import PageTemplate from "../../../../components/PageTemplate";
import SlidesContainer from "../../../../components/SlidesContainer";

export const EventSlidePage = () => {
  return (
    <PageTemplate>
      <SlidesContainer />
    </PageTemplate>
  );
};

export default EventSlidePage;
